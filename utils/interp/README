This file contains details for building the interpolation program interp. To
configure and build:

1) Set parameters in interp.f90

2) Set compiler parameters in Makefile.in in the top lesgo directory.

2) Build interp by running:

     make
