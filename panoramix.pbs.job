#!/bin/bash
#PBS -q batch

#PBS -l nodes=1:ppn=4

#PBS -l walltime=24:00:00

echo $PBS_JOBID

cd $PBS_O_WORKDIR

export MPI_NPROCS=` wc -l $PBS_NODEFILE | awk '{print $1}'`

mpiexec -np $MPI_NPROCS ./lesgo > lesgo.out


